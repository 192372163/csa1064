name: Flask CI/CD Pipeline
on:
  push:
    branches: ["main"]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Login to DockerHub
        run: echo "Successfully logged into DockerHub (Mocked)"
      - name: Build Docker Image
        run: echo "Successfully built Docker image (Mocked)"
      - name: Push Docker Image
        run: echo "Successfully pushed Docker image (Mocked)"
